# Project Architecture Rules (Non-Obvious Only)

- Static site generated by mdBook; no server-side logic
- Content organization follows SUMMARY.md; all new pages must be linked there
- Event files use consistent pattern: header, description, code blocks with @[event] syntax
- Custom styling only in src/css/custom.css; avoid modifying theme files